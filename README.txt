Please write small people search engine web page.
Page will fetch the data from external API:
 http://jsonplaceholder.typicode.com/users
When the data is loading, please show some indicator (spinner or something…).

After loading,
 show the basic info about users (name, surname, email, city) in rows.
 Please implement basic filter using INPUT field (if empty, show all users, filter by name or by email).
 When one have clicked on the row with user data,
   please open the modal window with some additional data (+3 other fields).
 When modal is opened, user should close the modal clicking outside of the modal or using ESC key.
